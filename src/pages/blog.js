import React from 'react';
import { Link } from 'gatsby';

const Blog = () => {
  return (
    <div>
      Blog Page works
      <Link to={'/'}>Go Home</Link>
    </div>
  );
};

export default Blog;
